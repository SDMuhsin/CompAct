torch>=2.0.0
triton>=2.1.0
transformers>=4.30.0
datasets>=2.14.0
accelerate>=0.20.0
numpy>=1.24.0
matplotlib>=3.7.0
tqdm>=4.65.0
evaluate
pandas
filelock
bitsandbytes
lion-pytorch
adapters==1.2.0          # ⚠ NOT `adapter-transformers` — that name now resolves to a
                         # DEPRECATED 4.0.0 STUB whose only content is "use adapters
                         # instead". It installs happily and provides NO `adapters`
                         # module, so `train_glue.py:101 import adapters` still fails.
                         # adapters 1.2.0 requires `transformers ~=4.51.3`, i.e. it
                         # reinforces our pin rather than fighting it.
peft
galore-torch
huggingface_hub[hf_xet]
sentencepiece
scikit-learn
protobuf
einops  # HyC-LoRA compute_utils (lazy; only for pad_input/unpad_input, unused by fused path)
